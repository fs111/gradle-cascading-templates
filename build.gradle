apply plugin: 'groovy'
apply plugin: 'idea'
apply plugin: 'maven'


project.version = "1.0-wip-dev"
project.group = "cascading"

repositories {
    ivy {
            artifactPattern 'http://tellurianring.com/projects/gradle-plugins/[module]/[revision]/[artifact]-[revision].[ext]'
            ivyPattern 'http://tellurianring.com/projects/gradle-plugins/[module]/[revision]/[artifact]-[revision].[ext]'
    }
    mavenCentral()
}

dependencies {
    compile 'gradle-templates:gradle-templates:1.3'
    compile gradleApi()
    groovy localGroovy()
}
